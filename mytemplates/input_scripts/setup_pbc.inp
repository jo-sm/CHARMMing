crystal defi {{ xtl_ucell }} {{ xtl_x }} {{ xtl_y }} {{ xtl_z }} {{ xtl_angles }}
crystal build noper 0

{% include 'input_scripts/zero_charge.inp' %}

define selvent sele resid BWAT .or. resid SOD .or. resid CLA .or. resid POT - 
CHARMM-scripts   .or. resid CES .or. resid CAL .or. resid MG end

image byres sele solvent end
image byseg sele .not. solvent end

! reset the non bond parameters to use particle mesh ewald (PME). For the grid size, we set
! it to the nearest value that is more than two times the box dimension that is only a multiple
! of 2, 3, or 5.
nbond inbfrq -1 imgfrq -1 atom vatom cdie eps 1.0 -
CHARMM-scripts elec ewald pmew fftx {{ ewalddim }} ffty {{ ewalddim }} fftz {{ ewalddim }} kappa 0.34 spline order 6 -
CHARMM-scripts vdw vswitch cutnb 16.0 cutim 16.0 ctofnb 12. ctonnb 10. wmin 1.0

